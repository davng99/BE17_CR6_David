{% extends 'base.html.twig' %}
{% block body %}

<div class="container">
    <h2 class="page-header mt-5 mb-5 text-center">Add Event</h2>
    {{form_start(form)}}
    {{form_widget(form)}}
    {{form_end(form)}}
    <a href="/" class="btn btn-danger">Back To Events</a>
</div>

{% endblock %}